# 사업 배경 요약 (새 세션에 붙여넣기용)

## 누구인가
공주대 부동산학과 1학년. 시행사(개발), 투자 리서치, 기관급 시장분석 쪽 커리어를 목표로 함.
장기적으로 대한민국 대표 부동산 인프라 기업("부동산 산업의 운영체제")을 만드는 것이 목표.

## 최종 확정된 사업 방향
**AI 부동산 인텔리전스 플랫폼 — 부동산 PF(프로젝트 파이낸싱) 사업성 심사 자동화 SaaS**

- 처음 구상은 "개인 재건축·재개발 투자자용 AI 리포트"였으나, 경쟁사 분석 후 피벗함
- 경쟁사(랜드북/스페이스워크, 디스코, 밸류맵, 부동산플래닛)는 전부 B2C·공인중개사 대상
  주소검색 → 실거래가/사업성 리포트 서비스를 이미 제공 중 — 이 카테고리는 레드오션
- 디스코(10년 업력, 누적투자 73억, 직원 12명, 최근 성장률 0%)가 보여주듯 이 시장은
  "100조 규모 독점 인프라"가 아니라 스몰~미드 마켓 비즈니스로 이미 검증 끝난 상태
- **피벗 결론**: 첫 타겟 고객을 개인 투자자가 아니라 **신탁사·캐피탈사·저축은행·
  자산운용사의 PF 심사역**으로 변경. 이들은 심사 리스크가 크고(건당 수십~수백억),
  소프트웨어 예산도 크며, 기존 프롭테크가 건드리지 않은 공백 시장.

## MVP 정의
주소 검색 → 데이터 자동 수집 → AI 분석 → PF 심사용 투자 리포트 생성 → PDF 다운로드.
그 외 기능(앱, 커뮤니티, 자체 거래, 대출, SNS, 광고)은 초기에 전부 제외.

## 지금까지 만든 것
`src/components/PFReportMVP.jsx` — 주소·대지면적·용도지역·사업유형·심사대상기관을
입력하면 사업성 지표(총사업비/분양수입/수익률/LTV/DSCR), 5개 리스크 매트릭스,
A~D 등급, AI 종합의견을 "신탁사 심사역이 보는 문서" 스타일로 생성하는 React 프로토타입.
현재는 용도지역별 평균 시세·용적률 가정 기반 계산 로직(목업)이며, 실거래가 API는
아직 연동 전.

## 진행 상황 업데이트
- 국토부 실거래가 7종 API 연동 코드 작성 완료, 사용자 소유 서비스키를 `.env.local`에 반영
- 주소 → 법정동코드(LAWD_CD) 변환기(`src/lib/lawdCodes.js`) 추가 — 현재 서울 25개 구만 지원,
  다른 지역은 code.go.kr에서 코드 찾아 테이블에 추가하면 됨 (지오코딩 API 없이 문자열 매칭 방식)
- `PFReportMVP.jsx`가 실행 시 실거래가를 먼저 시도하고, 실패하면 기존 가정치 로직으로
  자동 폴백하도록 리팩터링 완료. 리포트 상단에 "국토부 실거래가 반영" / "가정치 기반 추정"
  배지로 어떤 데이터를 썼는지 항상 표시함
- 로컬(Node) 환경에서 `test-api.mjs`로 키 동작 여부는 사용자가 직접 확인 완료

## 다음 단계 (이어서 할 일)
1. 서울 25개 구 외 지역(경기·인천 등) LAWD_CD 확장 — code.go.kr에서 코드 찾아 `lawdCodes.js`에 추가
2. V-World 용도지역 API 실제 연동 (별도 키 발급 필요 — README.md 참고), 지금은 용도지역을
   사용자가 직접 입력하는 방식
3. 첫 고객 확보 채널 구체화 (신탁사·캐피탈 심사역에게 어떻게 접근할지)
4. 가격/유닛 이코노믹스 정의 (기관용 라이선스 단가, CAC/LTV)
5. AI가 만든 "사업성 분석 리포트"가 자본시장법상 유사투자자문업으로 해석될
   소지가 있는지 법률 검토
6. 시장 규모 정량화 (신탁사/캐피탈사 수, 연간 PF 심사 건수 등으로 TAM 추정)

## 2026-07-21 안정성 점검 및 수정 (완료)
- 숫자 입력값 검증 추가(`validateForm`) — 대지면적/자기자본비율/대출기간/분양률/금리/수수료가
  비거나 범위를 벗어나면 실행 전 에러 표시, 이전엔 리포트 전체가 조용히 NaN으로 깨졌음
- `runAnalysis`에 대출금액 발산 가드 추가 — 고금리+장기 대출 조합에서 대출금액이 음수/무한대로
  튀던 버그, 이제 `financialModelInvalid`로 감지해 등급 D + 경고 배너 표시
- `tryFetchRealPrice`에 `import.meta.env.DEV` 가드 추가 — 프로덕션 빌드에서는 프록시가 없어
  항상 실패하는데, 이제 그 사유를 명확히 안내(이전엔 원인불명 네트워크 에러)
- `lawdCodes.js`의 전남광주통합특별시(2026-07-01 출범) 관련 코드는 미검증 상태로 주석 표시함
- `xlsx` npm audit 취약점(high 2건, CVE-2023-30533/CVE-2024-22363)은 **읽기(파싱) 경로 전용
  취약점**이라 확인함 — 이 프로젝트는 export(쓰기)만 하므로 현재는 실제 위험 없음. 추후
  "엑셀 업로드로 가져오기" 같은 읽기 기능을 추가하면 그때 재점검 필요

### 보류된 항목 (사용자 결정 대기, 2026-07-21 확인)
- **API 키 노출 문제**: 실거래가/브이월드 키가 `VITE_` 접두사라 프로덕션 빌드 시 클라이언트
  JS에 평문으로 노출됨(실제 확인함). 배포 플랫폼(Vercel 서버리스 / 별도 Express 백엔드 등)이
  정해지면 그때 프록시를 서버 쪽으로 옮겨야 함 — 지금은 로컬 데모 단계라 보류
- **vite 5→8 업그레이드**: esbuild 취약점(moderate, dev 서버 한정) 수정 가능하나 breaking
  change라 보류. 다른 이유로 vite를 업그레이드할 때 같이 처리

## 창업 원칙 (계속 유지)
- 문제를 먼저 해결하고 기술은 그다음이다
- AI는 기능이며, 데이터와 워크플로가 제품이다
- 작은 시장에서 압도적인 제품을 만든 뒤 확장한다
- 고객이 시간을 아끼고 더 나은 결정을 내리게 하는 것이 가장 큰 가치다
- 아이디어보다 실행과 고객 검증을 우선한다
